# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# Build artifacts
*.zip
*.tar.gz
build/
dist/

# Dependencies
node_modules/
vendor/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / local
.env
.env.*
!.env.example

# PHP
.phpunit.result.cache
.phpunit.cache/

# WP / plugin dev
.phan/
.psalm/

# AGENTS.md — local-only testing, never commit CI
# Test runner configs belong here, not in the repo.
/phpunit.xml.dist
/phpcs.xml.dist
/phpunit.xml
/phpcs.xml
/.wp-env.json

# CI workflows — never committed (AGENTS.md)
/.github/workflows/

# Local test sources — never committed (AGENTS.md)
/tests/
